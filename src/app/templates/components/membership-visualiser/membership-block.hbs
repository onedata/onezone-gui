{{#if isCondensed}}
  {{one-icon
    class="type-icon"
    icon=iconName
  }}
  {{#truncated-string class="record-name"}}{{record.name}}{{/truncated-string}}
{{else}}
  {{record-info-with-icon
    record=record
    icon=iconName
  }}
{{/if}}