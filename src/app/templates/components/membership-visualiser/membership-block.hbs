{{#if isCondensed}}
  <OneIcon @class="type-icon" @icon={{iconName}} />
  <TruncatedString @class="record-name">{{record.name}}</TruncatedString>
{{else}}
  <RecordInfoWithIcon @record={{record}} @icon={{iconName}} />
{{/if}}