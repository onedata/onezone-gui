<ProceedProcessModal
  @opened={{opened}}
  @close={{close}}
  @proceed={{action "proceed"}}
  @processing={{processing}}
  @modalClass={{concat-classes "select-model-modal" modalClass}}
  @headerText={{headerText}}
  @proceedButtonClass={{or proceedButtonClass "btn btn-primary proceed"}}
  @proceedButtonText={{proceedButtonText}}
  @proceedDisabled={{proceedDisabled}}
>
  <p class="input-top-description">
    {{messageText}}
  </p>
  <div class="dropdown-container">
    <PromiseProxyContainer @proxy={{records}} @sizeClass="sm">
      <OneDropdown
        @options={{recordsForDropdown}}
        @selected={{selectedRecord}}
        @onChange={{action (mut selectedRecord)}}
        @matcher={{action "recordMatcher"}}
        @searchEnabled={{true}}
        @placeholder={{dropdownPlaceholder}}
        as |record|
      >
        <div class="option-container">
          <TruncatedString>
            <OneIcon @icon={{recordIcon}} />
            {{name-conflict item=record tagName="span" class="text record-name"}}
          </TruncatedString>
        </div>
      </OneDropdown>
    </PromiseProxyContainer>
  </div>
</ProceedProcessModal>
