{{#promise-proxy-container proxy=viewModel.entries.initialLoad}}
  {{!-- FIXME: sprawdzić, czy błędy pierwszego załadowania są obsługiwane --}}
  {{#if isEmpty}}
    {{content-spaces-marketplace/empty viewModel=viewModel}}
  {{else}}
    {{content-spaces-marketplace/content viewModel=viewModel}}
  {{/if}}
{{/promise-proxy-container}}