<OneIcon
  class="upload-object-error-icon text-danger clickable"
  @icon="checkbox-filled-warning"
  onclick={{action "showErrorModal"}}
>
  {{#if errorMessage}}
    <OneTooltip @title={{errorMessage}} />
  {{/if}}
</OneIcon>
