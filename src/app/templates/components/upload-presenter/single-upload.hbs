<UploadPresenter::UploadSummaryHeader
  @uploadObject={{uploadObject}}
  @onToggleExpand={{action "toggleExpand"}}
  @onCancel={{action "cancel" uploadObject}}
  @onToggleMinimize={{action "toggleMinimize"}}
  @isMinimized={{isMinimized}}
/>
<RenderLater @triggerRender={{or triggerChildrenRender isExpanded}}>
  <OneCollapse @collapsed={{not isExpanded}}>
    <UploadPresenter::UploadObjectRootList
      @uploadObjects={{uploadObject.children}}
      @onCancel={{action "cancel"}}
    />
  </OneCollapse>
</RenderLater>
