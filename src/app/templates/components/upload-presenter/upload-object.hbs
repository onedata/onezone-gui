<UploadPresenter::UploadObjectInfo
  @uploadObject={{uploadObject}}
  @nestingStyle={{nestingStyle}}
  @isExpanded={{isExpanded}}
  @onToggleExpand={{action "toggleExpand"}}
  @onCancel={{action onCancel uploadObject}}
/>
<RenderLater @triggerRender={{triggerChildrenRender}}>
  <OneCollapse @collapsed={{not isExpanded}}>
    {{#each uploadObject.children as |child|}}
      <UploadPresenter::UploadObject
        @nestingStyle={{nestingStyle}}
        @uploadObject={{child}}
        @onCancel={{onCancel}}
        @onExpand={{action "childrenExpanded" child}}
      />
    {{/each}}
  </OneCollapse>
</RenderLater>
