{{#loading-container
  isLoading=modelProxy.model.isPending
  errorReason=modelProxy.model.reason}}
  {{one-dynamic-tree
    class="border-dashed"
    definition=initialTreeState
    compareValues=persistedPrivileges
    overrideValues=overridePrivileges
    disabledFieldsPaths=treeDisabledPaths
    valuesChanged=(action "treeValuesChanged")}}
  {{yield}}
{{/loading-container}}
