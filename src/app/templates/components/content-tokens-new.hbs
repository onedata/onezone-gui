<OneCarousel @activeSlideId={{activeSlide}} as |carousel|>
  <carousel.slide @slideId="templates">
    <div class="row content-row header-row hidden-xs">
      <h1>{{tt this "header"}}</h1>
    </div>
    <div class="row content-row">
      <TokenTemplateSelector @onTemplateSelected={{action "templateSelected"}} />
    </div>
  </carousel.slide>
  <carousel.slide @slideId="form">
    <div class="row content-row header-row hidden-xs">
      <h1 class="with-back relative nowrap">
        <a class="content-back-link" {{action "backToTemplates"}}>
          <OneIcon @icon="tokens" class="content-back-resource-icon" /><OneIcon
            @icon="arrow-left"
            class="content-back-arrow-icon"
          />
        </a>
        <div class="resource-name">
          {{tt this "header"}}
            <small class="template-name">
              <TruncatedString>
                {{tt this "fromTemplateHeader"
                  templateName=(t (concat "components.tokenTemplateSelector.templates." lastSelectedTemplateName ".title"))
                }}
              </TruncatedString>
            </small>
        </div>
      </h1>
    </div>
    <div class="row content-row">
      <TokenEditor
        @mode="create"
        @token={{lastSelectedTemplate}}
        @onSubmit={{action "submit"}}
      />
    </div>
  </carousel.slide>
</OneCarousel>
