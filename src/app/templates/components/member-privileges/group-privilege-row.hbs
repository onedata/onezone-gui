<td
  onclick={{action "changeOpenGroup"}}
  class={{concat-classes
    "priv-group"
    (if isModified "modified-priv")
  }}
>
  <div class="group-privilege-text-container">
    {{#if isOpen}}
      <OneIcon @icon="arrow-up" />
    {{else}}
      <OneIcon @icon="arrow-down" />
    {{/if}}
    <div>{{privilegesGroup.text}}</div>
  </div>
</td>
{{#if isDirect}}
  <MemberPrivileges::GroupPrivilegesCell
    @privileges={{privileges}}
    @groupPrivilegeName={{privilegesGroup.text}}
    @inputChanged={{action "inputChanged"}}
    @form={{form}}
    @isDirect={{true}}
    @editionEnabled={{editionEnabled}}
    @newGrantedEffPrivCount={{0}}
    @arePrivilegesUpToDate={{true}}
    @isUnknownEffPrivStatus={{false}}
    @isPrivilegesToggleDisabled={{isPrivilegesToggleDisabled}}
    @targetRecordType={{targetRecordType}}
  />
{{else}}
  <td class="toggle-column direct group na">{{tt this "na"}}</td>
{{/if}}
{{#if (not isBulkEdit)}}
  {{#if this.isUnknownEffPrivStatusProxy.isPending}}
    <td class="group-privileges-cell">
      <LoadingContainer
        @isLoading={{true}}
        @sizeClass="xxs"
        @spinnerBlockClass="effective-loading-container"
      />
      {{#if this.effectiveLoadingTip}}
        <OneTooltip @title={{this.effectiveLoadingTip}} />
      {{/if}}
    </td>
  {{else}}
    <MemberPrivileges::GroupPrivilegesCell
      @privileges={{effectivePrivilegeValues}}
      @groupPrivilegeName={{privilegesGroup.text}}
      @inputChanged={{action "inputChanged"}}
      @form={{form}}
      @isDirect={{false}}
      @newGrantedEffPrivCount={{newGrantedEffPrivCount}}
      @arePrivilegesUpToDate={{arePrivilegesUpToDate}}
      @isUnknownEffPrivStatus={{isUnknownEffPrivStatus.content}}
      @isPrivilegesToggleDisabled={{isPrivilegesToggleDisabled}}
      @targetRecordType={{targetRecordType}}
      @effectiveLoadingTip={{this.effectiveLoadingTip}}
    />
  {{/if}}
{{/if}}