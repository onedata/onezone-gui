<div class="world-map light">
  <div class="public-content">
    <div class="container">
      <h1 class="public-title">{{header}}</h1>
      <WysiwygEditor @mode="show" @content={{content}} />
    </div>
  </div>
  <PublicFooter>
    <div class="back-to-main-page">
      <LinkTo @route="index">
        <OneIcon @icon="home" />
        {{#unless (media "isMobile")}}
          <span>{{tt this "backToMainPage"}}</span>
        {{/unless}}
      </LinkTo>
    </div>
  </PublicFooter>
</div>
