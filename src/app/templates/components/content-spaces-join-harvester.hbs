{{#content-info
  class="scroll-breakpoint-700"
  header=(tt this "header")
  imagePath="assets/images/space-join-harvester.svg"
  text=(tt this "text" spaceName=space.name)
  buttonLabel=(tt this "joinButton")
  buttonAction=(action "joinHarvester" token)
  buttonDisabled=(not token)
  as |contentInfo|
}}
  <div class="row">
    <div class="col-xs-12">
      <input
        id="join-harvester-token"
        value={{token}}
        oninput={{action (mut token) value="target.value"}}
        onkeydown={{invoke-on-enter contentInfo.buttonAction}}
        placeholder={{tt this "placeholder"}}
        disabled={{contentInfo.actionInProgress}}
        class="form-control join-harvester-token"
      >
    </div>
  </div>
{{/content-info}}
