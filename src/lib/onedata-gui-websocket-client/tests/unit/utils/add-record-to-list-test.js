import { expect } from 'chai';
import { describe, it } from 'mocha';
import saveRecordInList from 'onedata-gui-websocket-client/utils/save-record-in-list';

describe('Unit | Utility | save record in list', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = saveRecordInList();
    expect(result).to.be.ok;
  });
});
