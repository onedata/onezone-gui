import { expect } from 'chai';
import { describe, it } from 'mocha';
import membersCollectionItemProxy from 'onezone-gui/utils/members-collection/item-proxy';

describe('Unit | Utility | members collection/item proxy', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = membersCollectionItemProxy();
    expect(result).to.be.ok;
  });
});
