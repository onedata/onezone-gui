// import { expect } from 'chai';
import { describe /*, it */ } from 'mocha';
import { setupComponentTest } from 'ember-mocha';
// import hbs from 'htmlbars-inline-precompile';

describe('Integration | Component | login box/loading container', function () {
  setupComponentTest('login-box/loading-container', {
    integration: true,
  });

  // TODO implement tests when backend endpoint will be available (to mock it).
});
